# Adobe Commerce Settings Instructions

- Configuration must contain only storefront-safe values.
- Never request or persist Adobe Commerce Admin tokens, integration secrets, or customer credentials.
- Core GraphQL remains required for mutations even when Catalog Service supplies product reads.
- Keep environment variable names and README documentation synchronized with configuration fields.
